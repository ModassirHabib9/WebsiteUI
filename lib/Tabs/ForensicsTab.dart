import 'package:flutter/material.dart';

class ForensicsTab extends StatelessWidget {
  const ForensicsTab({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Forensics Tab'),
      ),
    );
  }
}

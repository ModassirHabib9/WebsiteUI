import 'package:flutter/material.dart';

class ManualScreeningTab extends StatelessWidget {
  const ManualScreeningTab({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Manual Screening Tab'),
      ),
    );
  }
}
